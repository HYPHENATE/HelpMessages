jobs:
  install_authenticate:
    executor: sfdx/default
    steps:
      - checkout
      - sfdx/install
      - sfdx/auth:
          defaultusername: daniel@hyphen8.com
      - run: 
            name: Create Scratch Org
            command: |
                sfdx force:auth:web:login -d -a DevHub
                sfdx force:org:create -v DevHub -s -f "config/$SCRATCH_DEF" -a scratch
      - run: 
            name: Push Source
            command: |
                sfdx force:source:push -u scratch
      - run:
            name: Run Apex Tests
            command: |
                mkdir ~/tests
                mkdir ~/tests/apex
                sfdx force:apex:test:run -u scratch -c -r human -d ~/tests/apex -w 9999
orbs:
  sfdx: circleci/salesforce-sfdx@1.0.0
version: 2.1
workflows:
  basic-test:
    jobs:
      - install_authenticate